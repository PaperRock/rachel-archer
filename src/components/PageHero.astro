---
interface Props {
	title: string;
	lead?: string;
}

const { title, lead } = Astro.props;
---

<section class="page-hero">
	<div class="container">
		<h1>{title}</h1>
		{lead && <p class="lead page-lead">{lead}</p>}
	</div>
</section>
