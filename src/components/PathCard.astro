---
import TextLink from "./TextLink.astro";

interface Props {
	title: string;
	href: string;
	label: string;
}

const { title, href, label } = Astro.props;
---

<article class="path">
	<h3>{title}</h3>
	<slot />
	<TextLink href={href} label={label} />
</article>
