---
interface Props {
	alt?: boolean;
}

const { alt = false } = Astro.props;

const classes = ["section", alt ? "section-alt" : ""].filter(Boolean).join(" ");
---

<section class={classes}>
	<div class="container">
		<slot />
	</div>
</section>
