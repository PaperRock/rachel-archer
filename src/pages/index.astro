---
import { getEntry } from "astro:content";
import PageLayout from "../layouts/PageLayout.astro";

const entry = await getEntry("pages", "index");

if (!entry) {
	throw new Error("Missing content entry: index");
}
---

<PageLayout entry={entry} />
